<div dir="rtl">

# 📷 آتلیه معین — سیستم مدیریت جامع

سیستم مدیریت حرفه‌ای آتلیه عکاسی و فیلمبرداری عروسی، ساخته‌شده با **Next.js 15** و **TypeScript**.

---

## ✨ امکانات

### مدیریت مراسمات
- رزرو سریع مراسم از طریق تقویم جلالی یا فرم مستقل
- پشتیبانی از چند مراسم در یک روز
- قرارداد کامل با جزئیات: نوع، تاریخ، ساعت، آدرس، مبلغ
- نمایش مبالغ با فرمت فارسی (جداکننده هزارتایی) و تبدیل عدد به حروف
- مدیریت وضعیت: رزرو / جاری / تکمیل / لغو

### مدیریت کارمندان
- پروفایل کامل با ساعت کاری، ساعت حضور، توضیحات شغلی
- لیست وظایف روزانه با نوار پیشرفت رنگی
- تخصیص کارمندان به هر مراسم با شرح نقش

### تقویم جلالی هوشمند
- نشان‌دهی رنگی روزها: آبی = ۱ مراسم، بنفش = ۲+ مراسم
- نشان مناسبت‌های رسمی ایرانی (نقطه قرمز + tooltip)
- رزرو سریع با کلیک روی روزهای خالی (مدیر)

### احراز هویت و نقش‌ها
- ورود امن با JWT ذخیره‌شده در HttpOnly Cookie
- نقش‌ها: `admin` / `accountant` / `employee`
- ریدایرکت خودکار بر اساس نقش

### تجربه کاربری
- **Dark Mode** بدون flicker با `next-themes`
- طراحی RTL کاملاً ریسپانسیو (موبایل / تبلت / دسکتاپ)
- اعلان‌های toast با Sonner (پشتیبانی از dark mode)
- PWA Ready با manifest و آیکون‌های مناسب

---

## 🛠️ تکنولوژی‌ها

| ابزار | نسخه | کاربرد |
|---|---|---|
| Next.js | 15.x | فریمورک اصلی (App Router) |
| TypeScript | 5.x | تایپ‌سیف |
| Prisma | 6.x | ORM با پایگاه داده SQLite |
| Tailwind CSS | 3.x | استایل‌دهی |
| next-themes | آخرین | Dark/Light mode |
| Sonner | آخرین | Toast notifications |
| jalaali-js | آخرین | تبدیل تاریخ شمسی |
| lucide-react | آخرین | آیکون‌ها |

---

## 🚀 راه‌اندازی محلی

### پیش‌نیازها
- Node.js >= 18
- npm یا pnpm

### مراحل نصب

```bash
# ۱. کلون مخزن
git clone https://github.com/your-username/atelier-moein-v2.git
cd atelier-moein-v2

# ۲. نصب پکیج‌ها
npm install

# ۳. ساخت پایگاه داده
npx prisma db push

# ۴. بارگذاری داده‌های اولیه (اختیاری)
npx ts-node prisma/seed.ts

# ۵. اجرای سرور توسعه
npm run dev
```

پس از اجرا، برنامه در آدرس [http://localhost:3000](http://localhost:3000) در دسترس است.

---

## 🔐 متغیرهای محیطی

فایل `.env` را در ریشه پروژه بسازید:

```env
# کلید مخفی برای JWT (حتماً تغییر دهید)
JWT_SECRET=your-very-secret-key-change-this

# مسیر پایگاه داده SQLite (پیش‌فرض)
DATABASE_URL="file:./dev.db"
```

> **هشدار:** هرگز مقدار `JWT_SECRET` را در مخزن عمومی قرار ندهید.

---

## 📋 دستورات

```bash
# سرور توسعه
npm run dev

# بیلد پروداکشن
npm run build

# اجرای بیلد پروداکشن
npm run start

# بررسی خطاهای TypeScript و Lint
npm run lint

# همگام‌سازی Prisma Schema با دیتابیس
npx prisma db push

# مشاهده دیتابیس در Prisma Studio
npx prisma studio
```

---

## 📁 ساختار پروژه

```
├── app/                    # Next.js App Router
│   ├── admin/             # داشبورد مدیریت
│   ├── employee/          # پنل کارمند
│   ├── login/             # صفحه ورود
│   ├── api/               # API Routes
│   │   ├── auth/          # احراز هویت
│   │   ├── ceremonies/    # مراسمات
│   │   ├── employees/     # کارمندان + وظایف
│   │   └── plans/         # پلن‌های خدمات
│   └── layout.tsx         # Layout اصلی
├── components/
│   ├── admin/             # کامپوننت‌های مدیریت
│   ├── layouts/           # Layout ها
│   └── ui/                # کامپوننت‌های UI (تقویم، DatePicker)
├── context/               # React Context (Auth, Theme)
├── lib/                   # ابزارها (Prisma، Auth، API Client)
├── prisma/
│   ├── schema.prisma      # مدل دیتابیس
│   └── seed.ts            # داده‌های اولیه
├── public/                # فایل‌های استاتیک
├── types/                 # تعاریف TypeScript
└── utils/                 # توابع کمکی (تبدیل عدد به حروف)
```

---

## 👤 کاربران پیش‌فرض (پس از seed)

| نقش | نام کاربری | رمز عبور |
|---|---|---|
| مدیر | `admin` | `admin123` |
| کارمند نمونه | `employee1` | `emp123` |

---

## 📸 اسکرین‌شات‌ها

> اسکرین‌شات‌ها به‌زودی اضافه می‌شوند.

---

## � راهنمای نصب و اجرا

### پیش‌نیاز

| نرم‌افزار | نسخه |
|-----------|-------|
| Node.js   | ≥ 18  |

---

### روش ۱ — اجرا از سورس (توسعه)

```bat
npm install
start.bat
```

سایت روی `http://localhost:3000` بالا می‌آید.

---

### روش ۲ — ساخت پکیج Deploy برای سیستم دیگر

```bat
node scripts/build-deploy.js
```

یک پوشه `deploy/` (~80 MB) ساخته می‌شود.  
این پوشه را به هر ویندوزی که Node.js دارد کپی کن → دابل‌کلیک `Start.bat`.

---

### روش ۳ — Production روی همین سیستم

```bat
Start Production.bat
```

لاگ‌ها در `logs/server.log` ذخیره می‌شوند.

---

### اطلاعات ورود پیش‌فرض

| نقش | نام کاربری | رمز عبور |
|-----|-----------|----------|
| مدیر | `admin` | `password123` |
| حسابدار | `accountant` | `password123` |

> **مهم:** بعد از اولین ورود رمز عبور را تغییر دهید.

---

### دیباگ روی گوشی واقعی

از موبایل از طریق IP شبکه وصل شوید (هنگام اجرا نمایش داده می‌شود):

```
http://192.168.x.x:3000
```

لاگ ورودها در `logs/access.log`:

```
[2026-04-19T10:23:11Z] LOGIN OK   user="admin" role=admin IP:192.168.20.15
[2026-04-19T10:23:15Z] LOGIN FAIL user="admin" IP:192.168.20.15
```

اگر از موبایل لاگین نمی‌شود:
1. `logs/access.log` را بررسی کنید
2. `LOGIN FAIL` → رمز اشتباه
3. هیچ entry → موبایل به سرور دسترسی ندارد (IP چک شود)
4. `LOGIN OK` ولی ریدایرکت نمی‌شود → مطمئن شوید `COOKIE_SECURE=false` در `.env` است

---

## 📄 لایسنس

MIT License — برای استفاده آزاد است.

</div>
